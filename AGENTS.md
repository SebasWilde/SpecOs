# AGENTS.md — [Project Name]

> Automatically read by Claude Code, Cursor, Copilot, Gemini CLI, OpenCode, and any compatible agent.
> This is the global project context for [Project Name].
> Generated by `/specos-init`. Update whenever the stack, team, or structure changes.

---

## Project context

[2-3 lines about what the product does and who it's for.]

---

## Specs structure

```
specs/
└── [feature-name]/
    ├── spec.md          ← journeys, ACs, technical — everything
    ├── tasks.md         ← subtasks by role, no checkboxes, no status fields
    ├── testcases.md     ← QA test cases with TC-XX IDs
    └── CHANGELOG.md     ← spec change history
```

- All specs live in `specs/`. Each feature has its own folder.
- No agent writes code without a `spec.md` present in this repo.
- Only the Lead role modifies `spec.md`.
- Repo presence = approved. No status field in the spec.

---

## Team and roles

| Role | Person | Responsibility |
|---|---|---|
| Lead | [name] | Writes specs, approves PRs, generates outputs |
| Backend | [name/s] | Implements API and business logic |
| Frontend | [name/s] | Implements UI and validations |
| QA | [name/s] | Writes and executes test cases |

> Roles are configurable. One person can hold multiple roles.
> `qa: agent` in specos-outputs.yml means the AI takes the breaking perspective automatically.

---

## Implementation repo paths

```
implementation_repos:
  backend: ../[repo-name-backend]
  frontend: ../[repo-name-frontend]
  e2e: null
```

> For monorepos: set all paths to `.` (current directory).
> Paths are stored locally in `session.md` — never committed.

---

## Tech stack

- **Backend:** [framework, language, version]
- **Database:** [engine and version]
- **Frontend:** [framework and version]
- **Infra:** [cloud, containers]
- **CI/CD:** [tool]

---

## Code conventions

- [Language]: [style guide, linter, formatter]
- Tests: [framework, minimum coverage]
- Commits: conventional commits — `feat/fix/chore/docs/refactor`
- PRs: reference the task ID — `[ID] #done` in the message

---

## What the agent MUST NEVER do

- Never write code without a `spec.md` present in this repo
- Never modify `spec.md` unless the active role is Lead
- Never change the database schema without creating a migration
- Never add dependencies without updating the dependencies file
- Never commit directly to `main` or `master`
- [add project-specific rules]

---

## SpecOS v3 skills

Run `/specos-start` to begin any session. The agent reads `session.md` and routes automatically.

| Skill | When to use |
|---|---|
| `/specos-start` | Begin any session — lead, dev, QA, or resume |
| `/specos-init` | First-time project setup (run once per project) |
| `/specos-lead` | Create or update a spec collaboratively |
| `/specos-dev` | Implement a task with full spec context |
| `/specos-qa` | Generate test cases from a spec |
| `/specos-distribute` | Generate outputs per specos-outputs.yml |
